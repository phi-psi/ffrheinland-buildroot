#!/bin/sh
# post-removal script.

echo "+ removing ff-fsm-update from crontab"
sed \
  -e "/\/sbin\/fsm watch update/d" \
  -e "/\/tmp\/p2ptbl\/update/d" \
  -i "${IPKG_INSTROOT}/etc/crontabs/root"

if [ -z "${IPKG_INSTROOT}" ]; then
  echo "+ restarting cron"
  /etc/init.d/cron restart
fi

